import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// render recebe 2 parâmetros: o que será renderizado e onde
ReactDOM.render(
  <App />,
  document.getElementById('root')
);
